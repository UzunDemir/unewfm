# 🎵 Next.js Synchronized Radio

Это минимальное радио, которое запускает одни и те же треки одновременно у всех пользователей.

## 🚀 Запуск локально

```bash
npm install
npm run dev
```
Затем открой [http://localhost:3000](http://localhost:3000)

## 📦 Деплой на Vercel

1. Загрузите папку проекта на [Vercel](https://vercel.com/).
2. Убедитесь, что файлы `track1.mp3` и `track2.mp3` находятся в папке `public/`.
3. После деплоя сайт автоматически синхронизирует воспроизведение по серверному времени.
